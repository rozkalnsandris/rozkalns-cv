# Managed by rpi5_cv_network_pin_07.sh
# Keeps the Cloudflare/UFW source subnet stable across Docker network recreation.
networks:
  default:
    name: cv_default
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 172.19.0.0/16
          gateway: 172.19.0.1
