name: C2 bootstrap evidence

on:
  pull_request:
    paths:
      - .github/workflows/c2-bootstrap.yml
      - tools/c2-rewrite.py
      - package.json
      - vite.config.mjs
      - frontend/**
      - scripts/build-frontend.mjs
      - scripts/check-frontend-dist.mjs
      - content/translations/**

permissions:
  contents: write

jobs:
  build:
    if: github.event.pull_request.head.ref == 'refactor/72-frontend-source-dist-v01'
    runs-on: ubuntu-24.04
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v6
        with:
          ref: refactor/72-frontend-source-dist-v01

      - uses: actions/setup-node@v6
        with:
          node-version: '24.18.0'

      - name: Apply one-shot repository migration
        shell: bash
        run: |
          set -Eeuo pipefail
          python3 tools/c2-rewrite.py
          python3 -m py_compile scripts/build-content.py
          bash -n scripts/validate-source.sh

      - name: Generate exact npm lock and build twice
        shell: bash
        run: |
          set -Eeuo pipefail
          npm install --package-lock-only --ignore-scripts --no-audit --no-fund
          npm ci --ignore-scripts --no-audit --no-fund
          npm run build:frontend
          first="${RUNNER_TEMP}/frontend-first"
          mkdir -p "$first"
          cp -a html/assets html/i18n html/index.html html/smarthome.html frontend-dist-manifest.json "$first/"
          npm run build:frontend
          diff -ruN "$first/assets" html/assets
          diff -ruN "$first/i18n" html/i18n
          cmp "$first/index.html" html/index.html
          cmp "$first/smarthome.html" html/smarthome.html
          cmp "$first/frontend-dist-manifest.json" frontend-dist-manifest.json
          npm run check:frontend
          printf 'C2_DOUBLE_BUILD_DETERMINISTIC=PASS\n'

      - name: Commit non-workflow migration and runner-generated dist
        shell: bash
        run: |
          set -Eeuo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A -- \
            package-lock.json frontend-dist-manifest.json \
            html/assets html/i18n html/index.html html/smarthome.html \
            scripts/build-content.py scripts/validate-source.sh
          if git diff --cached --quiet; then
            printf 'C2_NON_WORKFLOW_TREE_ALREADY_CURRENT=PASS\n'
            exit 0
          fi
          git commit -m 'refactor: separate frontend source from generated dist'
          git push origin HEAD:refactor/72-frontend-source-dist-v01

      - name: Upload lock and generated dist
        uses: actions/upload-artifact@v7
        with:
          name: c2-bootstrap-${{ github.sha }}
          path: |
            package-lock.json
            frontend-dist-manifest.json
            html/assets/
            html/i18n/
            html/index.html
            html/smarthome.html
          if-no-files-found: error
          retention-days: 7
