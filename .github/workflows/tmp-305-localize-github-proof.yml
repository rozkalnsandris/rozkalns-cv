name: tmp-305-localize-github-proof

on:
  push:
    branches: [fix/305-localize-github-proof]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Check out branch
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -Eeuo pipefail
          auth="$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 -w0)"
          git init .
          git remote add origin "https://github.com/${GITHUB_REPOSITORY}.git"
          git -c "http.https://github.com/.extraheader=AUTHORIZATION: basic ${auth}" fetch --no-tags origin fix/305-localize-github-proof
          git checkout -B fix/305-localize-github-proof FETCH_HEAD

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: '24.18.0'
          package-manager-cache: false

      - name: Patch, build and validate
        env:
          PYTHONPYCACHEPREFIX: ${{ runner.temp }}/rozkalns-cv-pycache
        run: |
          set -Eeuo pipefail
          python3 .github/tmp-305-builder.py
          npm ci --ignore-scripts --no-audit --no-fund
          npm run build:frontend
          npm run check:frontend
          python3 -m unittest tests.test_github_project_links
          node --test tests/frontend.test.mjs
          node --check tests/browser-smoke.mjs
          chrome_bin="$(command -v google-chrome || command -v google-chrome-stable || command -v chromium || command -v chromium-browser)"
          test -n "$chrome_bin"
          CHROME_BIN="$chrome_bin" node tests/browser-smoke.mjs
          rm -rf node_modules
          bash scripts/validate-source.sh "$PWD"

      - name: Commit final diff
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -Eeuo pipefail
          git rm .github/tmp-305-builder.py .github/workflows/tmp-305-localize-github-proof.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: localize GitHub proof labels'
          auth="$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 -w0)"
          git -c "http.https://github.com/.extraheader=AUTHORIZATION: basic ${auth}" push origin HEAD:fix/305-localize-github-proof
